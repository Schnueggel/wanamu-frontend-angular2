/**
 * This is the config for testing and to have an interface. Later this should be aliased by the webserver to a script that reads the config from the environment
 */
export const  WU_CONFIG = {
    apiUrl: 'http://localhost:9999/v1'
};